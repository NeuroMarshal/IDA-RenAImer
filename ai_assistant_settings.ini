[General]
ollama_url=http://127.0.0.1:11434
auto_discover_models=true
available_models="[]"
system_prompt="You are an experienced reverse-engineer. Analyse the Hex-Rays pseudocode and rename **every** identifier: the function itself, every parameter and local variable (a1, v1, Block …), every called function and every C-structure. Use clear descriptive **snake_case** names and comments (2-4 sentences). Return exactly one JSON object with the fields: {entrypoint, comment, variables, functions, structures}."
user_prompt="Return STRICTLY one valid JSON object (no markdown, no commentary!) with the structure: {entrypoint, comment, variables, functions, structures}. In 'variables' include ALL parameters (a1, a2, ...) and local variables (v1, v2, v3, Block, ptr, ...). Represent every rename as [{\"original_name\": \"a1\", \"new_name\": \"buffer_size\"}, {\"original_name\": \"v2\", \"new_name\": \"result_ptr\"}] and use snake_case."
debug=0
comment_max_line_length=128
max_called_functions=8
max_calling_functions=4
function_context_length=800
max_context_tokens=6000
display_thinking=true
display_reasoning=true
rename_structs=true
rename_called_functions=true
max_structures=5
structure_context_length=500
num_ctx=16384
n_batch=512
num_predict=16384
temperature=0.1
seed=0
thinking_mode=false
